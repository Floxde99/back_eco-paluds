model Partner {
  partner_id    Int         @id @default(autoincrement())
  name          String
  service_type  String
  coverage_area String
  capacity      Int
  rate          Float
  contact       String
  status        String
  transports    Transport[]
}
